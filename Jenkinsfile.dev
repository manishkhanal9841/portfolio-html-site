pipeline {
    agent any

    stages {
        stage('Code Fetch') {
            steps {
                echo 'Code fetch from git'
                git url:'https://github.com/manishkhanal9841/portfolio-html-site.git', branch:'master'
            }
        }
        
        stage('Build') {
            steps {
                sh 'docker build -t my-new-website-dev .'
            }
        }
        
        stage('Deploy') {
            steps {
                echo 'Code Deploy to container'
                sh 'docker kill my-new-website-dev'
                sh 'docker run -d -p 81:80 my-new-website-dev'
            }
        }
        
        
    }
}
